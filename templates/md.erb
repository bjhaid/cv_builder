#<%= contact.delete("name")["value"] %> : Résumé

##CONTACT:

<% if contact["email"] %>
[<%= contact["email"]["value"] %>](mailto:<%= contact.delete("email")["value"] %>)
<% end %>
<% contact.each do |k, v| %>
<% if v["link"] %>
[<%= v["value"] %>](<%= v["value"] %>)
<% else %>
<%= v["value"] %>

<% end %>
<% end %>

##EXPERIENCE:
<% work.each do |job| %>
_<%= job["title"] %>_ **<%= job["employer"] %>** <%= job["location"] %> <%= job["dates"] %>
<%= job["note"] %>

<% unless job["description"].nil? %>
<% job["description"].each do |item| %>
<% unless item.nil? %>
* <%= item.gsub(/<b>(.*)<\/b>/, "**\\1**") %>
<% end %>
<% end %>
<% end %>

<% end %>

##SKILLS:
<% skills.each do |skill| %>
  - **<%= skill['name'] %> **: <%= skill['skills'].join(", ") %>
<% end %>

##PROJECTS:
<% projects.each do |project| %>
  - **<%= project['name'] %> **:
    Role: <%= project['role'] %>
    Link: [<%= project['link'] %>](<%= project['link'] %>)
    Description: <%= project['description'] %>
<% end %>

##EDUCATION:
<% education.each do |institution| %>
<%= institution["degree"] %>.
<%= [institution["name"], institution["location"], institution["dates"]].compact.join(", ") %>

<% end %>

##AWARDS:
<% awards.each do |institution| %>
<% institution["awards"].each do |award| %>
- **<%= award["date"] %> <%= institution["name"] %>** (<%= institution["location"] %>): <%= award["name"] %>
<% end %>

<% end %>

This Resume was built with cv_builder(<https://github.com/bjhaid/cv_builder/>)
