#<%= contact["name"] %> : Résumé

##CONTACT:

[<%= contact["email"] %>](mailto:<%= contact["email"] %>)  
<%= contact["phone"] %>  
<% unless contact["page"].nil? %>
<<%= contact["page"] %>/>   
<% end %>
<<%= contact["twitter"] %>/>  
<<%= contact["github"] %>/>  
<<%= contact["linkedin"] %>/>  
<<%= contact["stackoverflow"] %>/>  

##EXPERIENCE:
<% work.each do |job| %>
_<%= job["title"] %>_ **<%= job["employer"] %>** <%= job["location"] %> <%= job["dates"] %>
<%= job["note"] %>

<% unless job["description"].nil? %>
<% job["description"].each do |item| %>
<% unless item.nil? %>
* <%= item.gsub(/<b>(.*)<\/b>/, "**\\1**") %>  
<% end %>
<% end %>
<% end %>

<% end %>

##SKILLS:
<% skills.each do |skill| %>
  * **<%= skill['name'] %> **: <%= skill['skills'].join(", ") %>
<% end %>

##EDUCATION:
<% education.each do |institution| %>
<%= institution["degree"] %>.  
<%= institution["name"] %>, <%= institution["location"] %> <%= institution["dates"] %>

<% end %>

##AWARDS:
<% awards.each do |institution| %>
<% institution["awards"].each do |award| %>
* **<%= award["date"] %> <%= institution["name"] %>** (<%= institution["location"] %>): <%= award["name"] %>
<% end %>

<% end %>

**References available on request.**
This Resume was built with cv_builder(<https://github.com/bjhaid/cv_builder/>)
